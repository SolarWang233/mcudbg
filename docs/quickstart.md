# Quickstart

This document is a placeholder for the first runnable `mcudbg` setup guide.

The initial version should cover:

1. Python environment setup
2. editable install
3. connecting `CMSIS-DAP`
4. connecting `UART`
5. loading an ELF
6. running the first startup-failure demo
